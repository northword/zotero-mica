name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: zotero-plugin-dev/workflows/setup-js@main
      - run: pnpm build
